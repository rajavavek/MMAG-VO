# Weights

No pretrained weights were included in the uploaded PDF. Store trained checkpoints here or pass checkpoint paths directly to the scripts.

Expected filenames after training:

```text
runs/mmag_full/best.pt
runs/vo_gru/best.pt
```
